import { Helmet } from "react-helmet-async";

const AddItem = () => {
    return (
        <div>
            <Helmet>
                <title>CraftXtore | Add</title>
            </Helmet>
            <p>Add</p>
        </div>
    );
};

export default AddItem;